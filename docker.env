# Database Configuration
DB_USER=coolanu_user
DB_PASSWORD=your-super-secure-password-change-this
POSTGRES_DB=coolanu

# PgAdmin Configuration
PGADMIN_EMAIL=admin@coolanu.com
PGADMIN_PASSWORD=admin123

# Database Connection
DB_HOST=localhost
DB_PORT=5432
DB_NAME=coolanu
DB_CONNECTION_STRING=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}

# Backup Configuration
BACKUP_RETENTION_DAYS=7
BACKUP_SCHEDULE=0 2 * * *  # Daily at 2 AM
S3_BACKUP_BUCKET=coolanu-db-backups
