# Suppress specific security vulnerabilities in build tools
# These are nested dependencies in react-scripts that cannot be safely updated
audit-level=high
fund=false
loglevel=warn

# Suppress specific package vulnerabilities that are in build tools only
# nth-check, postcss, webpack-dev-server are not runtime dependencies
audit-level=high
